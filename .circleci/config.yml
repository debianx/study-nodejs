version: 2.1

jobs:
  build-tests:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run:
          name: Install npm dependencies
          command: |
            npm install --save

# workflows:
#   build-tests:
#     jobs:
#       - run_tests